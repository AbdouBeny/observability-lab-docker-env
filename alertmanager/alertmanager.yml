global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'service']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'web-console-or-default'

receivers:
  - name: 'web-console-or-default'
    # tu pourras y ajouter un webhook Slack ou Discord ici !
